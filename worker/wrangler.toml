name       = "napkiq-automation"
main       = "src/index.ts"
compatibility_date = "2024-01-01"

account_id = "11de51483dbed991a23c44341f0ca00d"

# Cron: daily at 03:00 UTC = 08:30 IST
[triggers]
crons = ["0 3 * * *"]

# Env vars — set secrets via:
#   wrangler secret put AUTOMATION_SECRET
#   wrangler secret put WASENDER_API_KEY   ← temporary testing; remove when Twilio is live
[vars]
SERVER_URL = "https://so-ta1t.onrender.com"

# ── Local dev overrides ──────────────────────────────────────────────────────
[env.development]
[env.development.vars]
SERVER_URL = "http://localhost:8080"
